/*#include "Marcador.h"
#include <iostream>
#include "jugador.h"
Marcador::Marcador() : nombre(""), ficha(' '), puntaje(0) {
    // Constructor vacío
}

Marcador::Marcador(string nombre, char ficha) : nombre(nombre), ficha(ficha), puntaje(0) {
    // Constructor con parámetros
}

void Marcador::aumentarPuntaje(int puntos) {
    puntaje += puntos;
}

void Marcador::mostrarMarcador() {
    cout << "Jugador: " << nombre << "\nFicha: " << ficha << "\nPuntaje: " << puntaje << std::endl;
}

*/
